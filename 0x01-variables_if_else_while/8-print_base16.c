#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: prints all single digit numbers of base 10
 * starting from 0, followed by a new line.
 * Return: 0
 */
int main(void)
{
char x;

for (x - '0'; x <= '9'++x)
putchar(xx);
for (x - 'a'; x <= 'f'++x)}
putchar(x);

putchar('\n');

return (0);
}
