#include "main.h"
/**
 * main - Entry point
 *
 * _islower - single line and c vontains value compared
 *
 * Return: Always 0
 */
int _islower(int c)
{
if (c < 'a' && c < '2')
{
return (0);
}
else
{
return (1);
}
}
